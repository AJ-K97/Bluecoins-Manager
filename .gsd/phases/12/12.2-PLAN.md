---
phase: 12
plan: 2
wave: 1
---

# Plan 12.2: Comprehensive Logic Testing

## Objective
Verify the correctness of all core business logic, including bank parsers, database commands, and conversational bot flows, using the synthetic data and mocking infrastructure.

## Context
- .gsd/ARCHITECTURE.md
- src/parser.py
- src/commands.py
- src/bot.py
- src/intents.py
- tests/data_gen.py

## Tasks

<task type="auto">
  <name>Verify Parser & Command Logic</name>
  <files>tests/test_parsers.py, tests/test_commands.py</files>
  <action>
    - Write unit tests for `src/parser.py` using synthetic HSBC/Wise CSVs and PDF samples.
    - Test `add_transaction`, `add_account`, and `add_category` from `src/commands.py`.
    - Verify that transaction addition correctly triggers auto-categorization or queuing based on confidence thresholds.
    - Test edge cases like duplicate transaction prevention and currency normalization.
  </action>
  <verify>pytest tests/test_parsers.py tests/test_commands.py</verify>
  <done>
    - [ ] Parsers correctly extract all fields from synthetic files.
    - [ ] Commands correctly update the database and handle integrity errors.
  </done>
</task>

<task type="auto">
  <name>Verify Conversational Bot Flows</name>
  <files>tests/test_bot_conversations.py, tests/test_intent_handling.py</files>
  <action>
    - Mock `Update` and `Context` objects to simulate Telegram messages.
    - Test `handle_chat_message` and `handle_nl_dispatch` for all intents (Add Account, Add Transaction, etc.).
    - Verify the `ConversationalManager` state transitions (e.g., waiting for name -> waiting for institution).
    - Assert that pre-filled entities from `IntentAI` correctly skip redundant prompt steps.
  </action>
  <verify>pytest tests/test_bot_conversations.py</verify>
  <done>
    - [ ] Bot correctly identifies and dispatches all 6+ intents.
    - [ ] Multi-step conversational states are correctly tracked and resolved.
  </done>
</task>

## Success Criteria
- [ ] 100% logic coverage for CSV/PDF parsers.
- [ ] All primary bot conversational flows (Add/Modify/List) are verified.
- [ ] Command logic is robust against invalid or malformed data.
