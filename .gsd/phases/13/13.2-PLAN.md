---
phase: 13
plan: 2
wave: 1
---

# Plan 13.2: Verification of AI Memory Persistence

## Objective
Verify that the schema changes correctly implement "Soft Detachment". When a transaction is deleted, its linked AI memory should remain in the database with a NULL foreign key, ensuring the data is preserved for future use.

## Context
- src/database.py
- src/commands.py

## Tasks

<task type="auto">
  <name>Create Persistence Test Script</name>
  <files>tests/test_persistence.py</files>
  <action>
    - Create a new test file `tests/test_persistence.py`.
    - Setup a mock transaction with a linked `AIMemory` entry.
    - Delete the transaction using the bot's deletion logic (or direct SQLAlchemy delete).
    - Query the `AIMemory` table and assert that the entry still exists but its `transaction_id` is None/Null.
  </action>
  <verify>
    PYTHONPATH=. ./venv/bin/pytest tests/test_persistence.py
  </verify>
  <done>
    Test passes, proving that AI memory is correctly detached but not deleted.
  </done>
</task>

## Success Criteria
- [ ] Logic test proves persistence after deletion.
