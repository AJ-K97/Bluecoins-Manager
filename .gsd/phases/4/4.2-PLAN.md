---
phase: 4
plan: 2
wave: 1
---

# Plan 4.2: Telegram Bot Parity

## Objective
Update the Telegram Bot to support core features currently only available in the CLI: listing accounts, viewing stats, and adding single transactions.

## Context
- `src/bot.py`: Currently only handles CSV uploads.
- `src/commands.py`: reused logic.

## Tasks

<task type="auto">
  <name>Add Account and Stats Commands</name>
  <files>src/bot.py</files>
  <action>
    Implement `/accounts` command:
    - Call `get_all_accounts` from `src/commands.py`.
    - Reply with list of accounts.
    
    Implement `/stats` command:
    - Call `get_queue_stats` from `src/commands.py`.
    - Reply with formatted stats of items needing review.
  </action>
  <verify>
    Run bot locally and test `/accounts` and `/stats` (requires Telegram token, or mock update object in test).
  </verify>
  <done>
    Bot responds to commands with correct data.
  </done>
</task>

<task type="auto">
  <name>Add Single Transaction Flow</name>
  <files>src/bot.py</files>
  <action>
    Implement `/add` command with conversation handler (ConversationHandler).
    - Step 1: `/add` -> Ask for Amount & Description (e.g. "50 Lunch at McD").
    - Step 2: Parse text (regex or simple split).
    - Step 3: Ask for Account (Buttons/Keyboard with available accounts).
    - Step 4: Confirm & Execute `add_transaction`.
    - Reply with "Added: [Date] [Desc] [Amount] [Category]".
  </action>
  <verify>
    Mock test `ConversationHandler` flow or manual test.
  </verify>
  <done>
    Can complete an `/add` flow in the bot.
  </done>
</task>

## Success Criteria
- [ ] `/accounts` lists accounts.
- [ ] `/stats` shows review queue status.
- [ ] `/add` allows creating a transaction interactively.
