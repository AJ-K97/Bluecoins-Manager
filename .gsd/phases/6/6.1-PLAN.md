---
phase: 6
plan: 1
wave: 1
---

# Plan 6.1: Core PDF Ingestion

## Objective
Enable the application to parse PDF bank statements using `pypdf` and Regex-based line extraction.

## Context
- `src/parser.py`
- `requirements.txt`

## Tasks

<task type="auto">
  <name>Add pypdf Dependency</name>
  <files>requirements.txt</files>
  <action>
    - Add `pypdf` to requirements.txt.
    - Run `pip install -r requirements.txt`.
  </action>
  <verify>python3 -c "import pypdf"</verify>
  <done>Library installed</done>
</task>

<task type="auto">
  <name>Implement PDF Parsing Logic</name>
  <files>src/parser.py</files>
  <action>
    - Import `pypdf`.
    - Create `parse_pdf(file_path)` method in `BankParser` (or separate class).
    - Logic:
        1. `reader = pypdf.PdfReader(file)`
        2. Extract text from all pages.
        3. Split into lines.
        4. Use Regex to find transaction lines (Date... Amount).
        5. Handle multi-line descriptions (append lines until next Date match).
    - Update main `parse` method to check `file_path.endswith('.pdf')`.
  </action>
  <verify>Create `tests/test_pdf_parser.py` with specific regex cases</verify>
  <done>Can extract list of dicts from a mock PDF text block</done>
</task>

## Success Criteria
- [ ] `pypdf` installed.
- [ ] `BankParser` accepts `.pdf` files.
- [ ] Transactions extracted correctly from standard text layout.
