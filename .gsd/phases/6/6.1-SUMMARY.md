# Summary 6.1: Core PDF Ingestion

## Achievements
- Installed `pypdf`.
- Implemented `parse_pdf` in `src/parser.py`.
- Added regex-based line extraction logic.

## Verification
- `test_pdf_parser.py`: PASSED (Mocked PDF reader used).
- Parser correctly routes `.pdf` files to `parse_pdf`.
