---
phase: 6
plan: 2
wave: 1
---

# Plan 6.2: Noise Cleaning

## Objective
Improve PDF parsing reliability by removing headers, footers, and non-transactional text before parsing.

## Context
- `src/parser.py`

## Tasks

<task type="auto">
  <name>Implement NoiseCleaner</name>
  <files>src/parser.py</files>
  <action>
    - Add `clean_noise(lines)` method.
    - Logic:
        - Remove empty lines.
        - Remove lines matching "Page \d of \d".
        - (Optional) Statistical: Remove lines appearing on every page.
  </action>
  <verify>Unit test with noisy sample text</verify>
  <done>Noise lines removed from output</done>
</task>

<task type="auto">
  <name>Update Config for PDF</name>
  <files>data/banks_config.json</files>
  <action>
    - Add `pdf_regex` field to bank config.
    - Example: `^(\d{2}/\d{2}/\d{4})\s+(.+?)\s+(-?\d+\.\d{2})$`
    - Update parser to use this regex if present.
  </action>
  <verify>Validate config JSON syntax</verify>
  <done>Config supports PDF regex definitions</done>
</task>

## Success Criteria
- [ ] Common headers/footers stripped.
- [ ] Bank config supports regex patterns for PDFs.
