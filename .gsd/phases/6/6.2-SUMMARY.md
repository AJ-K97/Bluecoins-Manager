# Summary 6.2: Noise Cleaning & Integration

## Achievements
- Implemented `clean_noise` in `src/parser.py` to strip "Page X of Y" and empty lines.
- Integrated noise cleaning into `parse_pdf`.
- Updated `data/banks_config.json` with `pdf_regex` for HSBC.

## Verification
- Syntax error in `parser.py` fixed and verified.
- Config JSON is valid.
